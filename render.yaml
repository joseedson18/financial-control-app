services:
  # Backend Service (Python/FastAPI)
  - type: web
    name: financial-control-backend
    runtime: docker
    rootDir: backend
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FRONTEND_URL
        value: https://financial-control-frontend.onrender.com
        # Update this after deploying the frontend
